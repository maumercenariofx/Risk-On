# Results / Build Log

Review notes for completed work. 1-4 lines per entry, list format, readable.

<!-- Soft cap ~6k tokens (bytes / 4). -->

- [2026-08-21] Repo movido a `C:\Users\mauri\Risk On`. Se movieron los 27 items de nivel superior (incluidos `.git`, `.env.local`, `.github`, `node_modules`) y se borró el origen vacío. Verificado: `git status` limpio, remoto intacto, `npm run build` compila todas las rutas sin error. Nada que tocar en GitHub ni Vercel — el remoto no cambia con un movimiento local.
- [2026-08-21] Editor del sistema de diseño en `docs/business/brand/color-palette.html` (178 KB, autocontenido). Papel `#0A0A0B` y tinta `#F5F5F2` conservados de producción; acento azure `#3D7BF7` y señal magenta `#EC4E88` añadidos deliberadamente fuera del arco 22°-157° de las bandas del índice, para que lo interactivo no se lea como risk-on/risk-off. Los seis chequeos de contraste pasan (separación de tono 100°, mínimo 90). Limitación: Geist no está en el catálogo del generador, la vista previa usa Inter/JetBrains Mono mientras producción sigue con Geist self-hosted.
- [2026-08-20] Scaffold de `docs/context` + `CLAUDE.md` desde cero. `CLAUDE.md` documenta el pipeline del view diario con sus tres respaldos, los pesos reales del índice (VIX 35 / DXY 22 / MOVE 18 / US10Y 15 / MXN 10) y apunta a la const `SYSTEM` de `lib/dailyView.js` como fuente de verdad del tono.
- [2026-08-21] Auditoría cruzada de 6 agentes (diseño, quant, narrativa, editorial, autenticidad, ideación) + Fase 0 aplicada. Los tres patrones sistémicos: múltiples fuentes de verdad para el mismo número, nada se persiste (todo se recalcula), y copy que promete lo que el código no cumple. Hallazgo central: `scripts/research-posturas.mjs:78` etiqueta las barras sin `meta.gmtoffset`, así que la serie `MXN=X` (Europe/London) va corrida un día — es el mismo bug que `lib/forwardReturns.js` corrigió el 2026-07-31 y que nunca se propagó. Con la corrección, la banda RISK-OFF cae de 76% a 58.3% (n=36, IC95 [42.2, 72.9], p=0.20) y un random-entry de 20,000 sims da p=0.51.
- [2026-08-21] Fase 0 (6 arreglos, `npm run build` limpio, verificado contra `npm run start` con curl): (1) texto legal traído de `Disclaimer.jsx` —que nunca se importó— al `Footer` bilingüe y legible, más aviso propio dentro de `PosturaRecord`; `Disclaimer.jsx` borrado. (2) Claim del 76% retirado de `BandEvidence` con nota que explica por qué. (3) CTA `#subscribe` arreglado en `Nav` y `RiskGauge`: la condición se pregunta al DOM, no al pathname. (4) `/api/unsubscribe` — XSS reflejado escapado y baja sin autenticar cerrada con paso de confirmación (el one-click POST de RFC 8058 intacto). (5) Fallback sintético de precios (18.10–18.42) borrado de `MarketsClient`, con estado "sin datos" explícito. (6) `Lang.jsx` arranca en ES y respeta `navigator.language`.
- [2026-08-21] `prior_bias` añadido al `.map()` de `app/indice/page.jsx:28`. La marca "≠ prior" que `PosturaRecord` promete al lector era estructuralmente imposible: el campo se descartaba ahí, así que `forwardReturns.js` siempre recibía `prior: null`. Dato incómodo que esto destapa: los 16 views con `prior_bias` coinciden 16/16 con `postura_bias`, así que la marca sigue sin aparecer — ahora por una razón honesta (nunca hemos discrepado del modelo), no por plomería rota.
- [2026-08-21] Fase 1 — corrección de `gmtoffset` en los 4 scripts de backtest (`research-posturas.mjs:78`, `calibrate-scale-floor.mjs:42`, `backtest-riskscore.mjs:46`, `backtest-taindex.mjs:42`) copiando el patrón de `lib/forwardReturns.js:29`. **Re-corrido el backtest: la banda RISK-OFF pasa de 76% a 58% (n=36) contra una base pro-peso de 56.9%.** El 76% era el corrimiento de un día. La salida CSV, que apuntaba a scratchpads de sesiones muertas y fallaba al escribir, ahora va a `data/backtest/` versionado.
- [2026-08-21] Fase 1 — una sola fuente de bandas. `send-daily` y `send-recap` cortaban en 25/50/75 contra los 32/49/67 de `lib/riskScore.js`; ahora ambos importan `riskBand` y solo conservan el color, que sí es propio del correo por el fondo crema. `alert-check` llamaba a `computeRiskScore` sin `curve`, renormalizando sobre 8 señales de 9 — corregido.
- [2026-08-21] Fase 1 — borrados `lib/riskIndex.js` (0 importadores), `TronCanvas.jsx` y `VoronoiBackground.jsx`: 961 líneas muertas. `README.md` y `CLAUDE.md` reescritos contra las 9 señales reales de `riskScore.js`, y el hallazgo del 76% marcado como RETIRADO con su reemplazo medido.
- [2026-08-21] Fase 1 — accesibilidad: guarda de `prefers-reduced-motion` en `RiskSphere` (congela tras armarse, no en el frame 0), `QuantLab` (+ gate de IntersectionObserver que le faltaba), el count-up de `RiskGauge` y `ReadingProgress` (que además pasó de animar `width` a `transform` con rAF-throttle). 97 colores de texto que reprobaban AA barridos a `#8A8A8E`; tokens `riskon`/`riskoff` corregidos en `tailwind.config.js` con ratios medidos, y token `line` nuevo para bordes de input (3.27:1 contra 1.26:1 de `edge`).
- [2026-08-21] Fase 1 — convención direccional del color unificada en `Ticker.jsx`: en pares del peso el color es relativo al PESO (USD/MXN a la baja = verde), como ya hacían `IntradaySpark`, `PosturaRecord` y `BandEvidence`; para índices, acciones y cripto se conserva arriba = verde.
- [2026-08-21] Fase 1 (cierre) — ventana de soporte/resistencia unificada en **10 días** (decisión de Mauricio). `rollingLevels()` de `app/api/market/route.js` era una copia byte a byte de `levels()` en `lib/technicals.js` salvo por el default, y de ahí salían las dos verdades: el correo publicaba 10d y `/analisis` 20d bajo la MISMA etiqueta. Se borró la copia, se exporta `LEVELS_PERIOD = 10` como ventana canónica y ambos endpoints devuelven ya lo mismo (verificado en vivo: 16.8781 / 17.1557 en los dos).
- [2026-08-21] Fase 1 (cierre) — banda CONGELADA al publicar: `lib/dailyView.js` escribe `band` y `band_cuts` en el front-matter, y `forwardReturns.js` la prefiere sobre `riskBand()`. Mata la reclasificación retroactiva (los cortes cambiaron el 13-jul y tres views saltaron de CONSTRUCTIVE a RISK-ON sin un día nuevo de mercado). Los 58 views anteriores no traen `band` y caen al recálculo — backfillearlos implicaría reescribir `content/` a mano y es decisión de Mauricio.
- [2026-08-21] Fase 1 (cierre) — libro mayor inmutable en `public/data/postura-ledger.json` vía `scripts/update-ledger.mjs`, cableado al final de `gen-daily.yml` con `continue-on-error`. Un veredicto escrito no se recalcula NUNCA. Reproduce el marcador publicado (21/26) y es idempotente entre corridas (hash idéntico). `posturaRecord()` lo prefiere sobre Yahoo y ahora sobrevive a un fallo de Yahoo en vez de devolver `null` — antes la página de credibilidad se caía cuando se caía un tercero.
- [2026-08-21] Fase 3 — memoria de continuidad para el redactor (`condicion`, `support`, `resistance` en `fetchPrevViews`, ventana 5→10 sesiones) más tres reglas duras: la condición de ayer manda, prohibido decir "el nivel que venimos marcando" si cambió, y si un view nombró un juez el siguiente da el veredicto. Rendición de cuentas de condicional a diaria (disparaba 0 de 58 veces). Esqueleto suelto: 2-5 secciones, 2-4 bullets, 250-700 palabras. Vetada la antítesis "X, no Y" (88 usos ES / 52 EN) y borrada del prompt la frase de ejemplo que el modelo copiaba 24 veces. Manual editorial cableado con triple fail-open (15 KB ≈ 3,800 tokens). Campo `nota_humana` desde `notas/<slug>.txt`. **DRY_RUN real en Actions: validador OK a la primera, 179s, memoria de 10 views cargada.**
- [2026-08-21] Fase 3 (bugs destapados por el DRY_RUN) — `fetchPosturaRecord` usaba `.slice(0, 20)`: al redactor se le decía 16/16 (100%) cuando el marcador real es 22/27 (81%), porque las 5 posturas fallidas caían fuera de la ventana. Y `posturaPrior.js` seguía citándole el 76% y el 62% retirados. Ambos corregidos.
- [2026-08-21] Fase 2 — `lib/stats.js` nuevo (Wilson, traslape de intervalos, n efectivo, potencia vs base conocida, fricción en centavos). `posturaRecord` calcula ahora el benchmark ingenuo sobre las mismas fechas y la magnitud a favor de la postura. `/indice` publica: 22/27 (81.5%) [63.3–91.8] contra «pro-peso todos los días» 21/27 (77.8%) [59.2–89.4], **los intervalos se traslapan y se dice**; retorno medio 0.55% y 0.43% neto de 2 centavos de spread; n efectivo de 5 y los ~761 días que faltarían para distinguir 62% de la base. Frontera visual explícita entre track record y backtest, IC por banda, la mitad reciente publicada, y `SignalAudit` nuevo con el IC de las 9 señales — incluido que carry y curva (17% del peso) son ruido y que el compuesto predice menos que el estiramiento, que ni siquiera está en el índice.
- [2026-08-24] Guarda de frescura del pulso (`sanitizePulse` en `lib/dailyView.js`) + endurecimiento de los dos prompts que dejaron pasar el error. Tres redes: fuente de más de 3 días, día de la semana incoherente con la fecha en el año en curso (atrapa el dateline reciclado), y USD/MXN citado a ≥8 centavos del spot verificado (descarta el pulso entero). Cuando cae un bullet se retira también la lista de fuentes: el titular tóxico viajaba dentro del slug de la URL. Fail-open en todas las rutas — cualquier excepción devuelve el pulso tal cual y un pulso vacío solo significa generar sin él. Verificado con `scripts/test-pulse-frescura.mjs` (11 casos, fixture = el pulso real del incidente, verde) y `npm run build` limpio.
- [2026-08-24] Corregido el view del día: se reescribió sobre el ángulo #1 real (IPC +2.14%, z 2.4) usando SOLO datos del digest, y se sacó todo lo que venía del pulso sin verificar. Score, señales, postura, condición y niveles quedaron intactos por ser determinísticos. Además, `Powell` → `Warsh` en los 7 views de junio y julio que lo nombraban como chair en funciones después del 22-may (26 menciones), cada uno con nota de corrección al pie en ES y EN. Verificado: los 59 archivos de `content/` parsean y renderizan, y en los 7 tocados ningún campo del front-matter cambió salvo por el nombre — titulares incluidos.
- [2026-08-24] Barrido de lo que quedaba de las 6 auditorías. CORTES: `PortfolioSection` (522 líneas de simulador con AAPL/TSLA), `QuantLab` (144k partículas que empujaban el glosario fuera de pantalla), y las APIs `portfolio`, `daily`, `asset` y `ticker-search` — verificado consumidor por consumidor. COLA DE DISEÑO: `articleStyle` h3 sin el conflicto mono/sans y a 13px, `RegimeStrip` con la altura como segundo portador (el color solo era ilegible en deuteranopía), `/about` con iconos SVG en vez de 📅+✉️+"in" y con h1 propio.
- [2026-08-24] Los 6 recaps semanales tienen URL (`/recap/[slug]`), sitemap, RSS mezclado por fecha y enlace permanente en el correo. Llevaban desde julio existiendo solo en la bandeja de entrada porque `lib/posts.js` filtraba `.md` en la raíz de `content/`.
- [2026-08-24] `/metodologia` creada: los 9 pesos vivían dentro de DOS acordeones anidados y `/suscribete` prometía "metodología pública" desde siempre. La tabla se LEE de `lib/riskScore.js`, no se transcribe — una tabla copiada a mano es cómo el README acabó documentando un índice con DXY que nunca existió.
- [2026-08-24] `/alertas` con puerta de entrada (Nav, sitemap) y dos claims corregidos: el h1 decía "en tiempo real" mientras la línea de abajo decía "cada pocos minutos", y se declara que es beta sin costo, porque la tabla inscribe con status 'trial' y no hay cobro ni precio en ningún lado del repo.
- [2026-08-24] SEO bilingüe: `/en/archive/[slug]` con hreflang RECÍPROCO + x-default, sitemap de 79 → 138 URLs con `<xhtml:link>` alternates, y enlace visible entre versiones. 59 views con ~25,700 palabras en inglés que Google no podía indexar porque solo existía una URL. `LangProvider` acepta `force` para que /en/* no dependa del toggle.
- [2026-08-24] README reescrito contra el producto real: la sección de "publicar a mano" describía un flujo que hoy rompería la idempotencia del bot; ahora documenta el pipeline, `notas/` y el `dry_run`. Corregidas las fuentes de datos (Yahoo dominante, Stooq fuera), `mxnVol` (realizada, ya no manual) y la nota de compliance (el producto central ES un correo).
